<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MauiApp13_03"
             x:Class="MauiApp13_03.ListaProdutoPage"
             Title="ListaProdutoPage">
    <VerticalStackLayout>
        <ListView x:Name="listaProduto">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Produto">
                    <ViewCell Tapped="ViewCell_Tapped">
                        <Grid >
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                                <Label Grid.Row="1" Text="{Binding Nome}"/>
                                <Label Grid.Row="2" Text="{Binding Preco}"/>
                                <Label Grid.Row="0" Text="{Binding Categoria}"/>
                        </Grid>    
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </VerticalStackLayout>
</ContentPage>